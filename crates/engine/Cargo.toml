[package]
name = "engine"
version = "0.1.0"
edition = "2021"

[dependencies]
storage = { path = "../storage" }
sql = { path = "../sql" }
vector = { path = "../vector" }
thiserror = "1"

[dev-dependencies]
tempfile = "3"
